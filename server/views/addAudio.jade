extends layout
block append content
  h1 #{room.id}
  p Select audio for room
  .audios
    for audio in audios
      .audio
        p #{audio.artist} - #{audio.title}
        form(action='/room/#{room.id}/audio', method='POST')
          input(type='hidden', value='#{audio.id}', name='id')
          input(type='submit', value='Add')
    
